@{
    Layout = "_LayoutAdmin";
    ViewData["Title"] = "List Author";
}

@model List<Author>

<div class="row">
    <div class="col-md-12">
        <ol class="breadcrumb float-sm-left">
            <li class="breadcrumb-item"><a href="/">Home</a></li>
            <li class="breadcrumb-item"><a href="/admin/author">Author</a></li>
            <li class="breadcrumb-item active">@ViewData["Title"]</li>
        </ol>
    </div>
</div>
<div class="row">
    <div class="col-md-12">
        <div class="card">
            <div class="card-header">
                <h3 class="card-title">Authors</h3>
                <div class="card-tools">
                    <a href="@Url.Action("Create", "Author")" class="btn btn-outline-success"><i class="fas fa-plus"></i> Add Author</a>
                </div>
            </div>
            <div class="card-body p-0">
                <table class="table table-striped projects">
                    <thead>
                        <tr>
                            <th style="width: 5%">
                                ID
                            </th>
                            <th style="width: 10%">
                                Avatar
                            </th>
                            <th style="width: 20%">
                                Name
                            </th>
                            <th style="width: 50%">
                                Description
                            </th>
                            <th style="width: 10%">
                                Actions
                            </th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var author in Model)
                        {
                            <tr>
                                <td>
                                    @Html.DisplayFor(modelItem => author.AuthorId)
                                </td>
                                <td>
                                    <img src="@Url.Content(author.Avatar)" width="50" height="50" />
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => author.Name)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => author.Description)
                                </td>
                                <td>
                                    <a href="@Url.Action("Edit", "Author", new { id = author.AuthorId })" class="btn btn-outline-primary btn-sm"><i class="fas fa-pencil-alt"></i> Edit</a>
                                    <a onclick="deleteAuthor(@author.AuthorId)" class="btn btn-outline-danger btn-sm"><i class="fas fa-trash"></i> Delete</a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

<script>
    function deleteAuthor(id) {
        if (confirm("Bạn có chắc chắn muốn xoá không?")) {
            // Send delete request to server
            $.ajax({
                url: "/Author/Delete/" + id,
                type: "POST",
                data: {
                    id: id
                },
                success: function (result) {
                    alert("Successfully");
                    location.reload();
                }
            });
        }
    }
</script>